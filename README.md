# WanAnimate ComfyUI RunPod Worker

Minimal public worker repo for RunPod Serverless. Source project generated this from `runpod/wanimate`.

- Dockerfile: `runpod/wanimate/Dockerfile`
- Models are expected on `/runpod-volume/models` and can be populated by submitting `{ "input": { "mode": "download_models" } }`.
